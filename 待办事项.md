# TODO

## Current Focus

Settle admin import tooling around safe auth before building Web admin screens.

## P2 Phases

### Phase P2-A: Docs and schema

- [x] Add customer trip schema
- [x] Add customer trip import batch schema
- [x] Add transfer sample JSON
- [x] Add charter sample JSON
- [x] Add mixed sample JSON
- [x] Add P2 customer system product doc

### Phase P2-B: claimCustomerInvite + customer_trip_access

- [x] Add claimCustomerInvite
- [ ] Move invite binding out of selectCustomerQuote
- [ ] Standardize customer_trip_access naming around `bind_mode`
- [ ] Make getCustomerTransportQuotes read only after authorized access

### Phase P2-C: getCustomerHome real aggregation stabilization

- [ ] Stabilize customer_trips aggregation
- [ ] Hide trip_only history after visible_until
- [ ] Render charter only if present

### Phase P2-D: Operator customer directory

- [ ] Add adminListCustomers
- [ ] Add adminUpdateCustomerList
- [ ] Add operator customer directory page

### Phase P2-E: JSON import

- [x] Add importCustomerTripJSON dry_run
- [x] Add import apply
- [x] Add audit logs

## Recent Update

### 2026-06-03

- Feature completed: made operator preview open the exact customer share-card view.
- Files changed:
  - `cloudfunctions/getOperatorCustomerHomePreview/index.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `待办事项.md`
- Notes:
  - `getOperatorCustomerHomePreview` now returns `customer_share_preview` for the customer share-card renderer while keeping `customer_home` for operator review.
  - Customer Home consumes `operatorCustomerSharePreview` once and enters `tripInviteMode`, matching the real customer invite branch.
  - Published previews render only `published_snapshot`; unpublished previews show the same waiting state customers would see.
  - The exact customer preview is read-only and does not create users, claim invites, or write `customer_trip_access`.
- Next recommended task:
  - Deploy `getOperatorCustomerHomePreview`, preview / upload the mini-program frontend, then QA published and unpublished operator share-card previews against real customer invite opens.

### 2026-06-03

- Feature completed: rendered complete customer trip overview, daily cards, hotels, flights, and transport summaries.
- Files changed:
  - `cloudfunctions/buildCustomerTripVisibleDraft/index.js`
  - `cloudfunctions/getOperatorCustomerHomePreview/index.js`
  - `cloudfunctions/getCustomerTripByInvite/index.js`
  - `cloudfunctions/getCustomerHome/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxml`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxss`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.js`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.wxml`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.wxss`
  - `docs/schema/customer-trip.schema.json`
  - `待办事项.md`
- Notes:
  - Snapshot v2 now includes normalized `trip_summary`, `daily_summary_cards`, `hotel_cards`, and `flight_cards`.
  - Hotel cards derive from both `itinerary_days[].hotel` and top-level hotel arrays, with dedupe by hotel/date/day.
  - Customer Home, operator customer-home preview, operator trip detail preview, and invite opening now read the same normalized customer-facing structure.
  - Daily cards are clickable and open the existing customer day-detail page with that day's full timeline, hotel, and transport summary.
- Next recommended task:
  - Redeploy `buildCustomerTripVisibleDraft`, `getOperatorCustomerHomePreview`, `getCustomerTripByInvite`, and `getCustomerHome`; rebuild and publish a trip snapshot, then QA full overview, daily cards, hotels, Day 8 flight card, operator preview parity, and unpublished waiting state.

### 2026-06-03

- Feature completed: added operator transport order health and repair UI.
- Files changed:
  - `cloudfunctions/getRequestDetail/index.js`
  - `miniprogram/pages/operator/request-detail/request-detail.js`
  - `miniprogram/pages/operator/request-detail/request-detail.wxml`
  - `miniprogram/pages/operator/request-detail/request-detail.wxss`
  - `docs/product/single-transfer-production-readiness-review.md`
  - `待办事项.md`
- Notes:
  - `getRequestDetail` now returns `transport_order` and `transport_order_health` for assigned / confirmed / completed requests or requests with a selected quote.
  - Operator Request Detail now shows whether the customer-visible assigned driver source is `transport_orders`, `fallback_driver_vehicle`, or `none`, including missing required snapshot fields.
  - The repair button reuses `selectDriverQuote` for the same selected quote, preserving the existing idempotent repair and audit-log path.
- Next recommended task:
  - Deploy `getRequestDetail` and ensure `selectDriverQuote` is current, preview / upload the mini-program frontend, then QA assigned requests with complete, missing, and incomplete `transport_orders/{request_id}` snapshots.

### 2026-06-03

- Feature completed: enriched customer trip snapshots for daily itinerary and hotel rendering.
- Files changed:
  - `cloudfunctions/buildCustomerTripVisibleDraft/index.js`
  - `cloudfunctions/getOperatorCustomerHomePreview/index.js`
  - `cloudfunctions/getCustomerTripByInvite/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `待办事项.md`
- Notes:
  - `buildCustomerTripVisibleDraft` now writes `snapshot_model_version = 2` with `trip_summary`, `daily_summary_cards`, and normalized `hotel_cards`.
  - Operator preview and customer invite reads normalize legacy snapshots into the same v2 contract without changing access rules.
  - Customer Home now shows hotel and transport badges on daily itinerary cards and uses normalized hotel cards for invite/preview rendering.
- Next recommended task:
  - Redeploy `buildCustomerTripVisibleDraft`, `getOperatorCustomerHomePreview`, and `getCustomerTripByInvite`, rebuild the draft for the imported trip, then re-preview the customer My Trip page.

### 2026-06-03

- Feature completed: made operator customer preview render the full My Trip page structure.
- Files changed:
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `待办事项.md`
- Notes:
  - Operator Customer Preview no longer forces imported trip previews through the simplified invite-card branch.
  - Customer Home now renders the normal My Trip page in operator preview mode, including hero, progress, today itinerary, full daily itinerary, hotels, flights, transport blocks, benefits, and advisor contact.
  - Operator preview remains read-only and does not create users or `customer_trip_access`.
- Next recommended task:
  - Recompile the frontend and verify Customer Preview Center -> select customer -> My Trip shows all imported Day 1-Day 8 itinerary nodes.

### 2026-06-03

- Feature completed: made operator customer preview render imported trips before draft snapshot is built.
- Files changed:
  - `cloudfunctions/getOperatorCustomerHomePreview/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `待办事项.md`
- Notes:
  - `getOperatorCustomerHomePreview` now falls back to customer-safe fields from `customer_trips` when `draft_snapshot` is still empty.
  - The fallback supports imported `itinerary_days`, hotels, flights, transfers, and charter services for operator-only preview.
  - Customer Home treats operator preview payload with trip content as renderable even when real customers would still see the unpublished waiting state.
- Next recommended task:
  - Redeploy `getOperatorCustomerHomePreview`, recompile the frontend, and retest Customer Preview Center -> select customer -> My Trip preview for a newly imported unpublished trip.

### 2026-06-03

- Feature completed: made operator customer-home preview show draft itinerary content on the customer page.
- Files changed:
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `待办事项.md`
- Notes:
  - Customer Home no longer hides operator preview draft content only because `customer_would_see = waiting`.
  - If the preview payload has day, hotel, flight, transfer, charter, or transport content, the customer page renders it for operator review.
  - The page now shows an operator-only banner explaining whether the preview is published or an unpublished draft.
- Next recommended task:
  - Recompile the frontend and verify unpublished imported trips show in operator preview while real customer share links still show the waiting state until publish.

### 2026-06-03

- Feature completed: fixed operator preview entry into the customer home tab page.
- Files changed:
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `待办事项.md`
- Notes:
  - Customer Home is a tabBar page, so Customer Preview Center now uses `wx.switchTab` instead of `wx.navigateTo`.
  - Customer Home consumes `operatorCustomerHomePreview` from `globalData` during `onShow`, which is the path triggered by `switchTab`.
  - The operator preview still does not create users or write `customer_trip_access`.
- Next recommended task:
  - Recompile the mini-program frontend and verify Customer Preview Center -> select customer -> My Trip tab shows the customer-facing preview.

### 2026-06-03

- Feature completed: made Customer Preview Center enter the real customer home page UI.
- Files changed:
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxml`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxss`
  - `miniprogram/pages/customer/home/home.js`
  - `待办事项.md`
- Notes:
  - Selecting an existing customer now opens `pages/customer/home/home` with an operator preview payload, so operators see the same customer-facing page structure instead of only the embedded preview block.
  - The customer page consumes operator preview payload from `globalData`; it does not call customer identity functions and does not create users or `customer_trip_access`.
  - Manual `进入客户看到的页面` remains available after loading a preview.
- Next recommended task:
  - Preview/upload the mini-program frontend and test Customer Preview Center -> select customer -> customer home page -> native back.

### 2026-06-03

- Feature completed: allowed existing customer selection to enter Customer Preview Center without manually typing `trip_id`.
- Files changed:
  - `cloudfunctions/getOperatorCustomerHomePreview/index.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxml`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxss`
  - `待办事项.md`
- Notes:
  - Selecting an existing customer now calls the preview flow even when no `trip_id` is typed.
  - `getOperatorCustomerHomePreview` can resolve the customer's default trip from active `customer_trip_access` and return that customer-home preview.
  - The selector now explains that typing `trip_id` is only needed when the operator wants to force a specific newly imported trip.
- Next recommended task:
  - Redeploy `getOperatorCustomerHomePreview`, preview/upload the mini-program frontend, then test selecting a customer with an active saved trip from Customer Preview Center.

### 2026-06-03

- Feature completed: allowed customers to open published multi-day trip invites.
- Files changed:
  - `cloudfunctions/getCustomerTripByInvite/index.js`
  - `cloudfunctions/getCustomerTripByInvite/package.json`
  - `cloudfunctions/saveCustomerTripToProfile/index.js`
  - `cloudfunctions/saveCustomerTripToProfile/package.json`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `待办事项.md`
- Notes:
  - Customer Home now supports `/pages/customer/home/home?trip_id=xxx&invite_code=xxx`.
  - `getCustomerTripByInvite` validates active trip invites or existing `customer_trip_access`, returns only sanitized `published_snapshot`, and returns a waiting state for unpublished trips.
  - Existing active customer users auto-save published shared trips to `customer_trip_access`; first-time viewers can view temporarily and optionally save with a display name.
- Next recommended task:
  - Deploy `getCustomerTripByInvite` and `saveCustomerTripToProfile`, preview the customer share path from `createCustomerTripInvite`, then verify temporary, saved, registered-customer, unpublished, and invalid-invite cases.

### 2026-06-02

- Feature completed: restored native secondary-page navigation for Customer Preview Center.
- Files changed:
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxml`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxss`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.json`
  - `miniprogram/pages/operator/dashboard/dashboard.js`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `待办事项.md`
- Notes:
  - Customer Preview Center now behaves like the other operator secondary pages with the native page back control.
  - Removed the custom in-page `返回运营` button and custom navigation style.
  - Dashboard and import-success entry use `wx.navigateTo` again so the native back stack works.
- Next recommended task:
  - Recompile the mini-program frontend and verify Operator Dashboard -> 客户界面预览 uses the native page back behavior.

### 2026-06-02

- Feature completed: restored operator return action before entering customer preview.
- Files changed:
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxml`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxss`
  - `待办事项.md`
- Notes:
  - The Customer Preview Center now shows `返回运营` while the operator is still selecting a customer or entering a trip id.
  - Once a customer-home preview is loaded, the return action is hidden and the page remains in no-back preview mode.
- Next recommended task:
  - Recompile the mini-program frontend and verify dashboard -> customer selector can return, while loaded preview mode has no return button.

### 2026-06-02

- Feature completed: changed Customer Preview Center entry to stack-reset launch.
- Files changed:
  - `miniprogram/pages/operator/dashboard/dashboard.js`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `待办事项.md`
- Notes:
  - Dashboard and import-success navigation now use `wx.reLaunch` to enter Customer Preview Center, matching the no-visible-back preview-mode requirement and avoiding page stack navigation failures.
  - Route failures now log to console and show a clear toast.
- Next recommended task:
  - Recompile the mini-program frontend and retry entering `客户界面预览` from Operator Dashboard.

### 2026-06-02

- Feature completed: added operator customer home preview center for imported trips.
- Files changed:
  - `cloudfunctions/getOperatorCustomerHomePreview/index.js`
  - `cloudfunctions/getOperatorCustomerHomePreview/package.json`
  - `cloudfunctions/getOperatorCustomerHomePreview/lib/auth.js`
  - `cloudfunctions/searchCustomersForOperator/index.js`
  - `cloudfunctions/importCustomerTripJSON/index.js`
  - `miniprogram/app.json`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.js`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxml`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.wxss`
  - `miniprogram/pages/operator/customer-home-preview/customer-home-preview.json`
  - `miniprogram/pages/operator/dashboard/dashboard.js`
  - `miniprogram/pages/operator/dashboard/dashboard.wxml`
  - `miniprogram/pages/operator/dashboard/dashboard.wxss`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `miniprogram/pages/operator/customer-import/customer-import.wxml`
  - `待办事项.md`
- Notes:
  - Operator Dashboard now has a dedicated `客户界面预览` entry.
  - The new preview center renders a Customer Home-shaped operator preview with temporary guest and existing-customer modes.
  - `getOperatorCustomerHomePreview` resolves customer identity server-side, returns masked `openid_display`, and does not create users, claim invites, or write `customer_trip_access`.
  - Import apply now routes to the Customer Preview Center and returns `next_route`.
  - `searchCustomersForOperator` supports `mode: "preview_selector"` with operator-only masked OpenID and preview ranking fields.
- Next recommended task:
  - Deploy `getOperatorCustomerHomePreview`, redeploy `searchCustomersForOperator` and `importCustomerTripJSON`, then preview the dashboard entry and import redirect in WeChat DevTools.

### 2026-06-02

- Feature completed: added customer trip share-card generation after publish.
- Files changed:
  - `cloudfunctions/createCustomerTripInvite/index.js`
  - `cloudfunctions/createCustomerTripInvite/package.json`
  - `cloudfunctions/createCustomerTripInvite/lib/auth.js`
  - `cloudfunctions/createCustomerTripInvite/lib/audit.js`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.js`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.wxml`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.wxss`
  - `待办事项.md`
- Notes:
  - Operators can generate and copy a customer trip share path from the trip detail page only after the trip is published.
  - The new cloud function writes `customer_trip_invites`, reuses active unexpired invites, and returns a customer-home share path with `trip_id` and `invite_code`.
  - Customer trip invite opening is not implemented in this step.
- Next recommended task:
  - Deploy `createCustomerTripInvite`, create `customer_trip_invites` if missing, and then implement customer-side invite opening via `getCustomerTripByInvite`.

### 2026-06-02

- Feature completed: added manual trip detail entry after customer trip import.
- Files changed:
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `miniprogram/pages/operator/customer-import/customer-import.wxml`
  - `待办事项.md`
- Notes:
  - After confirmed import writes, the result panel now shows `查看行程详情` as a fallback entry into the operator trip detail page.
  - The automatic navigation now surfaces route failures so operators can tell when the preview package or page registration is stale.
- Next recommended task:
  - Recompile the mini-program, confirm write a trip, then open the operator trip detail page from the result button if automatic navigation does not fire.

### 2026-06-02

- Feature completed: added operator customer trip preview and publish page.
- Files changed:
  - `miniprogram/app.json`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.js`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.wxml`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.wxss`
  - `miniprogram/pages/operator/customer-trip-detail/customer-trip-detail.json`
  - `待办事项.md`
- Notes:
  - Operator import now navigates to the trip detail page after apply succeeds.
  - The new operator trip detail page uses existing `getOperatorTripPreview`, `buildCustomerTripVisibleDraft`, and `publishCustomerTrip` functions to load state, build a customer-safe draft, preview draft vs published snapshots, and publish reviewed content.
  - This step does not create customer share cards or customer trip-detail pages.
- Next recommended task:
  - Deploy `getOperatorTripPreview`, `buildCustomerTripVisibleDraft`, and `publishCustomerTrip`, preview the operator trip detail page, then add `createCustomerTripInvite` after publish is verified.

### 2026-06-02

- Feature completed: improved operator trip import failure diagnostics.
- Files changed:
  - `cloudfunctions/importCustomerTripJSON/index.js`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `待办事项.md`
- Notes:
  - `importCustomerTripJSON` now catches unexpected runtime errors and returns a structured failure response instead of letting the mini-program only show a generic call failure.
  - The operator import page now displays the underlying cloud function `errMsg`, `error_message`, or `error_code` so deployment, permission, and runtime issues can be diagnosed from WeChat DevTools.
- Next recommended task:
  - Redeploy `importCustomerTripJSON`, preview the operator import page, retry dry-run/apply, and use the displayed error text if the call still fails.

### 2026-06-02

- Feature completed: fixed operator trip import preview validation and customer assignment picker.
- Files changed:
  - `cloudfunctions/importCustomerTripJSON/index.js`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `miniprogram/pages/operator/customer-import/customer-import.wxml`
  - `miniprogram/pages/operator/customer-import/customer-import.wxss`
  - `待办事项.md`
- Notes:
  - Canonical `mixed` trip imports now treat `flights`, `transfers`, and `charter_services` as meaningful sections, so multi-day itinerary payloads are not rejected only because they use plural trip-level sections.
  - The operator import sample now includes a minimal itinerary day so dry-run preview succeeds out of the box.
  - The operator import access section now supports searching and selecting an existing customer via `searchCustomersForOperator` instead of manually typing `customer_user_id`.
- Next recommended task:
  - Preview operator import in WeChat DevTools, select an existing customer, and verify dry-run/apply create `customer_trip_access`.

### 2026-06-02

- Feature completed: added customer trip publish backend.
- Files changed:
  - `cloudfunctions/publishCustomerTrip/index.js`
  - `cloudfunctions/publishCustomerTrip/package.json`
  - `cloudfunctions/publishCustomerTrip/lib/auth.js`
  - `cloudfunctions/publishCustomerTrip/lib/audit.js`
  - `待办事项.md`
- Notes:
  - Operators can publish a reviewed customer trip draft by copying `draft_snapshot` into `published_snapshot`.
  - Publishing requires an existing `draft_snapshot` and blocks when `critical_warning_codes` are present.
  - Publish sets `review_status: "approved"`, `visibility_status: "published"`, increments `published_version`, and records reviewer / publisher timestamps.
  - This step does not create trip invites or customer trip-detail pages.
- Next recommended task:
  - Add `createCustomerTripInvite` so a published trip can generate a customer share card.

### 2026-06-02

- Feature completed: added backend customer trip draft build and operator preview functions.
- Files changed:
  - `cloudfunctions/buildCustomerTripVisibleDraft/index.js`
  - `cloudfunctions/buildCustomerTripVisibleDraft/package.json`
  - `cloudfunctions/buildCustomerTripVisibleDraft/lib/auth.js`
  - `cloudfunctions/buildCustomerTripVisibleDraft/lib/audit.js`
  - `cloudfunctions/getOperatorTripPreview/index.js`
  - `cloudfunctions/getOperatorTripPreview/package.json`
  - `cloudfunctions/getOperatorTripPreview/lib/auth.js`
  - `cloudfunctions/getOperatorTripPreview/lib/audit.js`
  - `待办事项.md`
- Notes:
  - Operators can build a customer-safe `draft_snapshot` from imported `customer_trips` without publishing it to customers.
  - `getOperatorTripPreview` returns the current draft, published snapshot if any, warning arrays, publish version, and changed-section summary without mutating trip visibility.
  - Customer visibility remains controlled by `published_snapshot` and `visibility_status`; this step does not create invites or customer pages.
- Next recommended task:
  - Add `publishCustomerTrip` so reviewed drafts can be promoted to `published_snapshot` after operator approval.

### 2026-06-02

- Feature completed: patched customer trip import preview/apply compatibility and initialized trip publish lifecycle fields.
- Files changed:
  - `cloudfunctions/importCustomerTripJSON/index.js`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `miniprogram/pages/operator/customer-import/customer-import.wxml`
  - `miniprogram/pages/operator/customer-import/customer-import.wxss`
  - `docs/schema/customer-trip.schema.json`
  - `待办事项.md`
- Notes:
  - `importCustomerTripJSON` now returns `preview_valid` and `can_apply` alongside legacy `valid` so the operator import UI works with canonical `schema_version: "1.0.0"` responses.
  - Canonical imports now persist `trip_id = external_trip_id` as a compatibility alias and initialize `review_status`, `visibility_status`, `warning_codes`, `critical_warning_codes`, `draft_snapshot`, `published_snapshot`, and `published_version`.
  - `docs/schema/customer-trip.schema.json` now supports richer multi-day itinerary items, flight segments, day-level transport / hotel summaries, time-conflict fields, and review warning codes.
  - Existing published snapshots are preserved on update; changed imported trips move back to review state instead of becoming customer-visible automatically.
  - Operator import UI now shows review / visibility / version state and warning codes, and no longer depends only on legacy `preview.valid`.
- Next recommended task:
  - Add `buildCustomerTripVisibleDraft` and `getOperatorTripPreview` so operations can preview the exact customer-facing trip page before publishing.

### 2026-06-02

- Feature completed: refined P4 customer trip home into an iOS-style day-of-travel hub.
- Files changed:
  - `cloudfunctions/getCustomerHome/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `miniprogram/pages/customer/day-detail/day-detail.js`
  - `miniprogram/pages/customer/day-detail/day-detail.wxml`
  - `miniprogram/pages/customer/day-detail/day-detail.wxss`
  - `待办事项.md`
- Notes:
  - Customer Home now renders the P4 charter hub in the order: trip progress, today driver / vehicle, today itinerary overview, and hotel.
  - `getCustomerHome` now includes a customer-safe `progress_strip` mock for the 091 itinerary while keeping `today_card.destination_cards` and `timeline_items` compatible.
  - Driver name / phone remain gated by `driver_visibility`; pending driver states show advisor-first copy instead of driver contact.
  - Day Detail keeps destination cards in the swiper and adds a separate hotel endpoint summary below the time chips.
- Next recommended task:
  - Preview Customer Home and Day Detail in WeChat DevTools for pending-driver, assigned-driver, no-hotel, and missing-progress states before connecting real trip-day aggregation.

### 2026-05-28

- Feature completed: read assigned transport orders by deterministic request id first.
- Files changed:
  - `cloudfunctions/getCustomerHome/index.js`
  - `cloudfunctions/getCustomerTransportQuotes/index.js`
  - `待办事项.md`
- Notes:
  - Customer-facing assigned driver reads now try `transport_orders/{request_id}` before the legacy `where(request_id).orderBy(updated_at)` query.
  - Responses include `assigned_transport_source` so QA can tell whether customer-visible driver details came from the order snapshot, driver/vehicle fallback, or no source.
  - Existing driver/vehicle fallback remains for incomplete legacy data, but the deterministic transport order snapshot is now the first read path.
- Next recommended task:
  - Deploy `getCustomerHome` and `getCustomerTransportQuotes`, then verify assigned transfer and charter pages report driver details from `transport_orders`.

### 2026-05-28

- Feature completed: added operator manual customer assignment and shared-invite auto-save hardening.
- Files changed:
  - `cloudfunctions/assignCustomerToRideRequest/index.js`
  - `cloudfunctions/assignCustomerToRideRequest/package.json`
  - `cloudfunctions/searchCustomersForOperator/index.js`
  - `cloudfunctions/searchCustomersForOperator/package.json`
  - `cloudfunctions/claimCustomerInvite/index.js`
  - `cloudfunctions/getRequestDetail/index.js`
  - `miniprogram/pages/operator/request-detail/request-detail.js`
  - `miniprogram/pages/operator/request-detail/request-detail.wxml`
  - `miniprogram/pages/operator/request-detail/request-detail.wxss`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.js`
  - `待办事项.md`
- Notes:
  - Operators can search active customer users and manually assign a ride request to an existing customer, creating a primary `customer_trip_access` row with `granted_source: "operator_manual"`.
  - `claimCustomerInvite` no longer treats `claimed_openid` as an exclusive card lock; multiple valid viewers can still open the invite, while saved visibility is controlled by `customer_trip_access`.
  - Existing registered customers now auto-save shared trips from both Customer Home and Transfer Detail without re-entering a display name.
- Next recommended task:
  - Deploy `assignCustomerToRideRequest`, `searchCustomersForOperator`, `getRequestDetail`, and `claimCustomerInvite`; then preview operator customer assignment plus registered-customer invite auto-save.

### 2026-05-28

- Feature completed: auto-claim advisor invite for registered customer profiles.
- Files changed:
  - `cloudfunctions/claimCustomerInvite/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `待办事项.md`
- Notes:
  - Registered `farland_profile` customers can open an advisor-shared trip card and automatically bind the trip to their existing Farland profile without re-entering a display name.
  - Unregistered or trip-only customers still need the explicit registration / save flow, so no new customer profile is created silently.
  - OPENID remains resolved inside `claimCustomerInvite`; the frontend only requests invite claiming and never passes identity.
- Next recommended task:
  - Deploy `claimCustomerInvite`, then preview an existing registered customer opening a new advisor invite card and confirm the trip appears in My Trip automatically.

### 2026-05-28

- Feature completed: hardened operator driver confirmation around `transport_orders`.
- Files changed:
  - `cloudfunctions/selectDriverQuote/index.js`
  - `待办事项.md`
- Notes:
  - `selectDriverQuote` no longer returns degraded success when the assigned transport snapshot cannot be written.
  - Confirmation writes `transport_orders/request_id` as the deterministic transport order document and returns `transport_order_saved: true` only after the snapshot path succeeds.
  - Runtime uses `db.startTransaction()` when available; otherwise it uses a fail-fast order that writes `transport_orders` before quote and request assignment updates.
  - Assigned / confirmed requests are idempotent when the same quote already has a transport order, and can repair missing assignment snapshots for the same selected quote.
- Next recommended task:
  - Deploy `selectDriverQuote`, then test a new transfer and a new charter assignment from operator confirmation through customer driver display.

### 2026-05-28

- Feature completed: show assigned driver details after operator confirmation.
- Files changed:
  - `cloudfunctions/getCustomerHome/index.js`
  - `cloudfunctions/getCustomerTransportQuotes/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `miniprogram/pages/customer/day-detail/day-detail.js`
  - `miniprogram/pages/customer/day-detail/day-detail.wxml`
  - `miniprogram/pages/customer/day-detail/day-detail.wxss`
  - `待办事项.md`
- Notes:
  - Assigned customer transport now treats an empty `transport_orders` snapshot as incomplete and falls back to `drivers` / `vehicles`.
  - Customer Home and Day Detail show driver, vehicle, and phone once Farland operations has confirmed assignment.
  - The rule applies to both `charter` and `transfer` service types; pre-assignment driver details remain hidden.
- Next recommended task:
  - Deploy `getCustomerHome` and `getCustomerTransportQuotes`, then preview customer home, day-detail, and transfer-detail after operator driver confirmation.

### 2026-05-27

- Feature completed: standardized customer-facing itinerary times to 24-hour format.
- Files changed:
  - `cloudfunctions/getCustomerHome/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/day-detail/day-detail.js`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.js`
  - `待办事项.md`
- Notes:
  - The 091 Day 1 mock now uses `08:10`, `10:00`, `13:40`, and `09:50`.
  - Customer Home, Day Detail, and Transfer Detail normalize AM/PM and 上午/下午 input strings before display.
  - This is display/data formatting only; no operator, driver, quote, or assignment logic changed.
- Next recommended task:
  - Preview customer home, day-detail, and transfer-detail to confirm all visible times use 24-hour format.

### 2026-05-27

- Feature completed: applied P4-3 customer itinerary page polish.
- Files changed:
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/day-detail/day-detail.js`
  - `miniprogram/pages/customer/day-detail/day-detail.wxml`
  - `miniprogram/pages/customer/day-detail/day-detail.wxss`
  - `待办事项.md`
- Notes:
  - Customer Home now hides empty Today, transport, hotel, and full-trip sections instead of stacking placeholder modules.
  - Legacy `todayItinerary` only renders when `todayCard` is missing and real legacy data exists.
  - Day Detail falls back from `destination_cards` to `timeline_items`, and shows a single advisor-focused fallback state if no cards exist.
  - Driver contact remains hidden from the legacy Today block; the default customer CTA is `联系顾问`.
- Next recommended task:
  - Preview empty home, 091 Day 1 home, and day-detail fallback states in WeChat DevTools before the next feature.

### 2026-05-27

- Feature completed: replaced the customer home next-trip card with a customer invite registration entry.
- Files changed:
  - `cloudfunctions/getCustomerHome/index.js`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `待办事项.md`
- Notes:
  - The hero area now shows a registration button instead of `下一项确认行程`.
  - Registration reuses the existing `claimCustomerInvite` path when the customer enters through an invite card.
  - The same button can also register a customer profile without an invite; this binds the current WeChat OPENID but does not create trip access.
  - OPENID binding remains inside the existing cloud function; the frontend does not pass `openid`.
  - Empty home no longer shows the explanatory line under `暂无行程安排`; it keeps only `联系顾问`.
- Next recommended task:
  - Deploy `claimCustomerInvite` and `getCustomerHome`, then preview invite registration and empty customer profile registration states.

### 2026-05-27

- Feature completed: changed customer advisor contact to WeChat QR code.
- Files changed:
  - `miniprogram/assets/images/advisor-wechat-qr.jpg`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `miniprogram/pages/customer/day-detail/day-detail.js`
  - `miniprogram/pages/customer/day-detail/day-detail.wxml`
  - `miniprogram/pages/customer/day-detail/day-detail.wxss`
  - `待办事项.md`
- Notes:
  - Customer Home and Today Detail now open a QR modal for `联系顾问`.
  - The QR image supports long-press recognition and tap-to-preview.
  - Advisor contact no longer depends on a phone number for the P4 customer itinerary path.
- Next recommended task:
  - Preview the QR modal on customer home and day-detail, then replace the QR asset if a different advisor code is required.

### 2026-05-27

- Feature completed: simplified customer home itinerary information architecture.
- Files changed:
  - `cloudfunctions/getCustomerHome`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `待办事项.md`
- Notes:
  - Customer Home now hides the legacy `我的用车` list when `todayCard` exists.
  - Empty home now renders one compact empty-state card instead of multiple empty sections.
  - `driver_visibility` is normalized so any non-`assigned` value hides driver contact details.
- Next recommended task:
  - Preview the empty, pending-driver, and assigned-driver customer home states before connecting real day resolution.

### 2026-05-26

- Feature completed: added P4 Today Itinerary detail page with destination-card swiper.
- Files changed:
  - `cloudfunctions/getCustomerHome`
  - `miniprogram/app.json`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `miniprogram/pages/customer/day-detail/day-detail.js`
  - `miniprogram/pages/customer/day-detail/day-detail.wxml`
  - `miniprogram/pages/customer/day-detail/day-detail.wxss`
  - `miniprogram/pages/customer/day-detail/day-detail.json`
  - `待办事项.md`
- Notes:
  - Customer Home now treats Today Card as a compact summary and entry point.
  - The old duplicate `todayItinerary` block is hidden when `todayCard` exists.
  - The new day-detail page renders one destination / itinerary node per swiper card.
  - `today_card.destination_cards` now carries the trip `2026XBC091` Day 1 mock.
- Next recommended task:
  - Preview customer home and day-detail in WeChat DevTools, then tune card density and navigation copy from screenshots.

### 2026-05-26

- Feature completed: added P4 Daily Itinerary Today Card mock and customer home rendering.
- Files changed:
  - `cloudfunctions/getCustomerHome`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `TODO.md`
- Notes:
  - `getCustomerHome` now returns a mock `today_card` for trip `2026XBC091` Day 1.
  - Customer home renders the Today Card above the legacy `今日行程` section while keeping existing sections intact.
  - Driver details remain hidden while `driver_visibility` is `pending`.
- Next recommended task:
  - Preview customer home in WeChat DevTools, review visual density, then decide whether to connect `today_card` to imported trip data.

### 2026-05-26

- Feature completed: made driver quick quote repeat submissions update the original quote.
- Files changed:
  - `cloudfunctions/submitQuickQuote`
  - `TODO.md`
- Notes:
  - Existing `driver_quotes` for the same `request_id` and `driver_id` are now updated instead of returning 409.
  - Repeat submissions reset the quote status to `submitted` and write `resubmitted_at` for operator review.
  - The response now includes `quote_id` for both create and update paths.
- Next recommended task:
  - Deploy `submitQuickQuote`, then test a driver opening the same quote card and updating the amount / note.

### 2026-05-26

- Feature completed: defaulted customer invite claims to trip-only access.
- Files changed:
  - `cloudfunctions/claimCustomerInvite`
  - `TODO.md`
- Notes:
  - Missing or unknown bind mode now resolves to `trip_only` instead of `farland_profile`.
  - Explicit `farland_profile` claims still require a display name and keep the persistent save behavior.
  - This aligns invite entry with the product direction that viewing a shared trip should not imply saving to My Farland.
- Next recommended task:
  - Deploy `claimCustomerInvite`, then test opening an invite without an explicit bind mode and saving explicitly to My Farland.

### 2026-05-23

- Feature completed: added assigned driver fallback from selected driver and vehicle records.
- Files changed:
  - `cloudfunctions/getCustomerTransportQuotes`
  - `cloudfunctions/getCustomerHome`
  - `TODO.md`
- Notes:
  - Customer read paths still prefer `transport_orders`, but now fall back to `ride_requests.selected_driver_id` / `selected_vehicle_id` when the assigned snapshot is missing.
  - The fallback is only used for assigned / confirmed requests and returns customer-safe driver / vehicle fields.
  - Customer frontend still does not read `drivers`, `vehicles`, or `driver_quotes` directly.
- Next recommended task:
  - Deploy `getCustomerTransportQuotes` and `getCustomerHome`, then retest assigned driver display for the request that already has `selected_driver_id`.

### 2026-05-23

- Feature completed: restored operator driver confirmation success when assigned snapshot write fails.
- Files changed:
  - `cloudfunctions/selectDriverQuote`
  - `TODO.md`
- Notes:
  - `selectDriverQuote` now updates `driver_quotes` and `ride_requests` before attempting the `transport_orders` customer-visible snapshot.
  - If snapshot write fails, operator confirmation still succeeds and writes `transport_order_snapshot_write_failed` to `audit_logs`.
  - The response includes `transport_order_saved` so QA can distinguish assignment success from customer snapshot failure.
- Next recommended task:
  - Deploy `selectDriverQuote`, retry confirmation, then inspect `transport_order_saved` and `audit_logs.transport_order_snapshot_write_failed`.

### 2026-05-23

- Feature completed: switched assigned transport order writes to deterministic request-id documents.
- Files changed:
  - `cloudfunctions/selectDriverQuote`
  - `TODO.md`
- Notes:
  - `saveTransportOrder` now writes `transport_orders/{request_id}` directly with `set`.
  - This removes the remaining query/add branch from operator confirmation and makes repeated confirmation attempts idempotent by request.
- Next recommended task:
  - Deploy `selectDriverQuote`, retry confirmation, and check `transport_orders` document id equals the request id.

### 2026-05-23

- Feature completed: removed indexed sort dependency from assigned transport order upsert.
- Files changed:
  - `cloudfunctions/selectDriverQuote`
  - `TODO.md`
- Notes:
  - `saveTransportOrder` no longer uses `orderBy('updated_at', 'desc')` when checking for an existing `transport_orders` row by `request_id`.
  - This avoids CloudBase index-related failures during operator driver confirmation.
- Next recommended task:
  - Deploy `selectDriverQuote`, retry confirmation, and verify `transport_order_id` is returned.

### 2026-05-23

- Feature completed: surfaced operator driver confirmation diagnostics in the Mini Program UI.
- Files changed:
  - `miniprogram/pages/operator/request-detail/request-detail.js`
  - `TODO.md`
- Notes:
  - Operator confirmation now shows a modal with `failed_step` / `error_code` when `selectDriverQuote` returns a structured failure.
  - Cloud function invocation failures now show the actual `errMsg` / message and log the full error to console.
- Next recommended task:
  - Preview / upload the Mini Program, retry confirmation, and use the modal content to identify whether the cloud function is stale, failing, or returning a business-state rejection.

### 2026-05-23

- Feature completed: added step-level diagnostics for operator driver confirmation failures.
- Files changed:
  - `cloudfunctions/selectDriverQuote`
  - `TODO.md`
- Notes:
  - `selectDriverQuote` now returns `failed_step` and writes `select_driver_quote_failed` audit logs when an unexpected exception occurs.
  - This distinguishes auth, quote loading, request loading, related quote loading, driver / vehicle resolution, `transport_orders` write, and final selection record update failures.
- Next recommended task:
  - Deploy `selectDriverQuote`, retry confirmation, and inspect the returned `failed_step` plus `audit_logs.select_driver_quote_failed` if it still fails.

### 2026-05-23

- Feature completed: relaxed operator driver confirmation status gate to avoid false selection failures.
- Files changed:
  - `cloudfunctions/selectDriverQuote`
  - `TODO.md`
- Notes:
  - `selectDriverQuote` now rejects only terminal / blocked request statuses for operator confirmation.
  - This keeps duplicate confirmation protection for assigned / confirmed requests while avoiding false blocks from historical or unexpected non-terminal statuses.
- Next recommended task:
  - Deploy `selectDriverQuote`, retest operator driver confirmation, and verify the response includes `transport_order_id`.

### 2026-05-23

- Feature completed: added assigned-driver read-path observability for RC QA.
- Files changed:
  - `cloudfunctions/getCustomerTransportQuotes`
  - `cloudfunctions/getCustomerHome`
  - `TODO.md`
- Notes:
  - Customer read paths now write audit logs when `transport_orders` reads fail.
  - Assigned / confirmed requests that have no readable assigned snapshot now write `transport_orders_missing_for_assigned_request`.
  - Customer-facing responses stay safe; diagnostics are recorded in `audit_logs` for operator / developer QA.
- Next recommended task:
  - Deploy `getCustomerTransportQuotes` and `getCustomerHome`, retest assigned-driver display, then inspect `audit_logs` if customer pages still show `待确认`.

### 2026-05-23

- Feature completed: aligned customer transfer detail assigned-driver display for assigned and confirmed states.
- Files changed:
  - `cloudfunctions/getCustomerTransportQuotes`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.js`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.wxml`
  - `TODO.md`
- Notes:
  - Customer transfer detail now reads and displays `transport_orders` when request status is `assigned` or `confirmed`.
  - Quote options are hidden for both finalized statuses, so customer detail and My Trip use the same assigned-driver state boundary.
- Next recommended task:
  - Deploy `getCustomerTransportQuotes`, preview / upload the transfer detail page, then retest confirmed requests with existing `transport_orders` rows.

### 2026-05-23

- Feature completed: allowed temporary invite viewers to select a customer quote without saving to My Farland.
- Files changed:
  - `cloudfunctions/selectCustomerQuote`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.js`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.wxml`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.wxss`
  - `TODO.md`
- Notes:
  - Valid invite links can now select a quote using the current WeChat OPENID without claiming the invite or creating `users` / `customer_trip_access`.
  - Selection records `selected_by_openid`, `selected_at`, `selected_access_source`, and non-binding request metadata.
  - Saving to My Farland remains optional and still uses `claimCustomerInvite`.
- Next recommended task:
  - Deploy `selectCustomerQuote`, preview / upload the customer transfer detail page, then test temporary selection and cross-WeChat conflict behavior.

### 2026-05-23

- Feature completed: fixed customer-visible assigned driver details after operator confirmation.
- Files changed:
  - `cloudfunctions/selectDriverQuote`
  - `cloudfunctions/getCustomerHome`
  - `TODO.md`
- Notes:
  - `selectDriverQuote` now writes a customer-safe `transport_orders` assignment snapshot when an operator confirms a driver.
  - `getCustomerHome` now reads assigned / confirmed `transport_orders` and returns `assigned_transport` for saved trip cards.
  - Customer pages still read assigned driver details from `transport_orders`, not directly from `driver_quotes`.
- Next recommended task:
  - Deploy `selectDriverQuote` and `getCustomerHome`, then create a fresh assigned request or backfill `transport_orders` for any already-assigned QA request before retesting customer pages.

### 2026-05-21

- Feature completed: added Mini Program operator customer trip import UI.
- Files changed:
  - `miniprogram/app.json`
  - `miniprogram/pages/operator/dashboard/dashboard.js`
  - `miniprogram/pages/operator/dashboard/dashboard.wxml`
  - `miniprogram/pages/operator/customer-import/customer-import.js`
  - `miniprogram/pages/operator/customer-import/customer-import.json`
  - `miniprogram/pages/operator/customer-import/customer-import.wxml`
  - `miniprogram/pages/operator/customer-import/customer-import.wxss`
  - `TODO.md`
- Notes:
  - Operator dashboard now links to a JSON import page.
  - Import page supports JSON paste, dry-run preview, optional access grant by `customer_user_id` or existing `request_id`, and confirmed write.
  - The page uses `wx.cloud.callFunction` only and does not directly access the database.
- Next recommended task:
  - Deploy `importCustomerTripJSON`, then test sample JSON dry-run and confirmed write in WeChat DevTools.

### 2026-05-21

- Feature completed: documented Web admin auth decision path before continuing admin-web.
- Files changed:
  - `docs/product/admin-web-auth-plan.md`
  - `AGENTS.md`
  - `TODO.md`
  - `admin-web/package.json`
- Notes:
  - Web admin cannot assume the Mini Program `wx.cloud.callFunction` OPENID chain.
  - Production `admin-web` screens are paused until a Web auth strategy is chosen.
  - Recommended next step is a Mini Program operator import UI first, because it reuses current operator OPENID and `requireRole`.
  - `admin-web/package.json` is only a placeholder scaffold; dependencies were not installed.
- Next recommended task:
  - Build `pages/operator/customer-import/customer-import` for JSON paste, dry-run preview, and confirmed write.

### 2026-05-21

- Feature completed: added `importCustomerTripJSON` dry-run trip import cloud function.
- Files changed:
  - `cloudfunctions/importCustomerTripJSON`
  - `docs/product/context.md`
  - `docs/product/p2-customer-system-and-trip-json.md`
  - `TODO.md`
- Notes:
  - Operator/super_admin can validate `customer-trip-v1` JSON with default `dry_run`.
  - Real writes require `dry_run: false`.
  - The function rejects sensitive customer-unsafe keys and does not accept frontend OPENID.
  - On write it upserts `customer_trips`, optionally grants `customer_trip_access` via `customer_user_id` or existing `request_id`, and writes an audit log.
- Next recommended task:
  - Add a minimal operator import UI with JSON paste, dry-run preview, and confirm write.

### 2026-05-21

- Feature completed: added `customer_trip_access` write/read path for customer home visibility.
- Files changed:
  - `cloudfunctions/claimCustomerInvite`
  - `cloudfunctions/getCustomerHome`
  - `docs/product/context.md`
  - `docs/product/p2-customer-system-and-trip-json.md`
  - `TODO.md`
- Notes:
  - `claimCustomerInvite` now creates or updates `customer_trip_access` after explicit customer confirmation.
  - `getCustomerHome` now reads active `customer_trip_access`, filters expired `visible_until`, reads `customer_trips`, and keeps migration fallback for older invite/request data.
  - Trip-only access expires in Cloud Functions rather than only being hidden in UI.
- Next recommended task:
  - Add `importCustomerTripJSON` with `dry_run` so operations can create `customer_trips` from validated JSON.

### 2026-05-21

- Feature completed: documented P2 customer system and standard trip JSON foundations.
- Files changed:
  - `AGENTS.md`
  - `docs/product/context.md`
  - `docs/product/p2-customer-system-and-trip-json.md`
  - `docs/schemas/customer-trip.schema.json`
  - `docs/samples/customer-trip-transfer.sample.json`
  - `docs/samples/customer-trip-charter.sample.json`
  - `TODO.md`
- Notes:
  - Security rules are now explicit: no frontend database access, no frontend OPENID, Cloud Function role checks, and no customer access to internal quote data.
  - Customer binding remains explicit with profile vs trip-only choice and required display name.
  - P2 data direction is `customer_trip_access`, `customer_trips`, `visible_until`, standard JSON, and `dry_run` imports.
  - Subpackages, image cloud compression, maps, payment, notifications, and external spreadsheet sync remain deferred.
- Next recommended task:
  - Implement `customer_trip_access` and update `getCustomerHome` to read real customer trips by access rules.

### 2026-05-21

- Feature completed: split customer invite claiming from read-only published quote access.
- Files changed:
  - `cloudfunctions/claimCustomerInvite`
  - `cloudfunctions/getCustomerHome`
  - `cloudfunctions/getCustomerTransportQuotes`
  - `miniprogram/pages/customer/home/home.js`
  - `miniprogram/pages/customer/home/home.wxml`
  - `miniprogram/pages/customer/home/home.wxss`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.js`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.wxml`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.wxss`
  - `TODO.md`
- Notes:
  - Customer invite claim is now explicit through `claimCustomerInvite` with profile vs trip-only binding.
  - `getCustomerTransportQuotes` no longer claims invites and no longer reads `driver_quotes`.
  - Customer Transfer Detail is read-only for P1.2A; it no longer calls `selectCustomerQuote`.
  - `getCustomerHome` no longer returns a fake Farland Guest itinerary for unbound users.
  - Operators can preview the customer homepage from request detail and return through an operator-only button.
- Next recommended task:
  - Deploy `claimCustomerInvite`, `getCustomerHome`, and `getCustomerTransportQuotes`, then preview the invite flow in WeChat DevTools.

## Immediate Stabilization

- [x] Restore operator/driver/customer role-based entry behavior
- [x] Add customer invite card flow from operator request detail
- [x] Add customer quote read-only detail backed by `customer_transport_quotes`
- [x] Add customer driver selection signal
- [x] Add operator confirmation after customer chooses a driver
- [x] Add operator driver-unavailable rejection path after customer selection
- [x] Show assigned driver details on customer page after operator confirmation
- [x] Show customer reselect notice after selected driver becomes unavailable
- [x] Split customer invite claim from quote read
- [x] Gate customer selection UI for read-only P1.2A
- [x] Add operator-only customer homepage preview return path
- [x] Add operator dashboard shortcut to hotel booking page
- [ ] Verify tabBar works in WeChat DevTools
- [ ] Verify image asset paths exist
- [ ] Test hotel request submission
- [ ] Test quick-quote token entry
- [ ] Test operator dashboard
- [ ] Test customer invite claim and read-only quote viewing on deployed cloud functions
- [ ] Confirm no frontend wx.cloud.database()

## Phase 0: Documentation

- [x] Add `AGENTS.md`
- [x] Add `docs/product/context.md`
- [x] Add `docs/product/farland-student-transport-itinerary-spec.md`
- [x] Add `docs/product/p1-1-data-boundary-customer-quotes.md`
- [x] Add `docs/product/p2-customer-system-and-trip-json.md`
- [x] Add `docs/schemas/customer-trip.schema.json`
- [x] Add transfer and charter customer trip JSON samples
- [x] Add initial `TODO.md`

## Phase 1: ICT Demo Data

- [x] Add mock trip data through `getCustomerHome`
- [x] Add mock daily itinerary data through `getCustomerHome`
- [x] Add mock transfer request data through `getCustomerHome`
- [x] Add mock transport quote data through `getCustomerHome`
- [x] Add mock transport order data through `getCustomerHome`
- [x] Add mock charter service data through `getCustomerHome`
- [x] Add mock activity events through `getCustomerHome`

## Phase 2: ICT Demo Client UI

- [x] My Trip page
- [x] Day View timeline
- [x] Transfer request card
- [x] Quote cards
- [x] Confirmed ride card
- [x] Charter display card
- [x] Transfer detail page

## Phase 3: ICT Demo Display Logic

- [x] Show request card before quote
- [x] Show quote options after published
- [x] Navigate from My Trip summary to Transfer Detail
- [x] Show driver details only after assignment
- [x] Show customer selected quote as pending operator confirmation
- [x] Replace quote options with assigned driver card after operator confirmation
- [x] Show driver-unavailable notice and allow customer reselection

## Long-Term Roadmap

These items are post-demo product direction. Do not implement them during ICT demo stabilization unless explicitly requested.

### Transfer Backend

- [ ] Add shared role helper before P1.1 backend work
- [ ] Create `transfer_request` backend
- [x] Add `reviewDriverQuote`
- [x] Add `createCustomerQuoteDraft`
- [x] Add `publishCustomerQuotesBatch`
- [ ] Add `withdrawCustomerQuotes`
- [x] Add `getCustomerTransportQuotes`
- [x] Add `selectCustomerQuote`
- [x] Publish curated `transport_quote` options through `customer_transport_quotes`
- [x] Select quote as customer intent signal
- [ ] Confirm `transport_order`
- [ ] Assign driver
- [x] Write audit logs for invite, approve/reject, draft, publish, read, and select
- [ ] Add audit logs for withdraw/order creation

### Long-Term Transport Product

- [ ] Production customer quote selection flow
- [ ] Transparent service fee quote flow
- [ ] Charter service backend
- [ ] Charter segment timeline integration
- [ ] Driver assignment release rules
- [ ] Customer notifications
- [ ] Payment
- [ ] Live map

## Phase 5: QA And Release

- [ ] Deploy `getCustomerHome`
- [ ] Deploy `createCustomerInvite`
- [ ] Deploy `claimCustomerInvite`
- [ ] Deploy `importCustomerTripJSON`
- [ ] Deploy `reviewDriverQuote`
- [ ] Deploy `createCustomerQuoteDraft`
- [ ] Deploy `publishCustomerQuotesBatch`
- [ ] Deploy `getCustomerTransportQuotes`
- [ ] Deploy `selectCustomerQuote`
- [ ] Deploy updated `getRequestDetail`
- [ ] Deploy updated `selectDriverQuote`
- [ ] Test customer hotel request flow
- [ ] Test My Trip mock data rendering
- [ ] Test Transfer Detail quote cards
- [ ] Test customer invite claim options
- [ ] Test read-only published quote viewing
- [ ] Test quote selection UI stays hidden in P1.2A
- [ ] Test operator sees customer selected quote
- [ ] Test operator confirms driver and customer sees assigned driver details
- [ ] Test operator marks driver unavailable and customer sees reselect compensation notice
- [ ] Regression test driver quick-quote token flow
- [ ] Regression test operator dashboard and request detail
- [ ] Confirm no frontend direct database access
- [ ] Confirm miniprogram package size under 2MB

## Change Log

### 2026-05-21

- Feature completed: updated README, TODO, and product context for current customer quote flow.
- Files changed:
  - `README.md`
  - `TODO.md`
  - `docs/product/context.md`
- Notes:
  - README now documents hotel, My Trip, customer invite, operator quote publishing, customer selection, and operator confirmation/rejection.
  - Product context now marks P1.2A customer quote flow as implemented and clarifies that customer selection is not a confirmed order.
  - TODO now reflects completed P1.2A flow and the next QA/deployment tasks.
- Next recommended task:
  - Commit and push the documentation updates after review.

### 2026-05-21

- Feature completed: implemented customer quote selection and operator confirmation flow.
- Commit:
  - `bc435ce Implement customer quote selection and operator confirmation flow`
- Files changed:
  - `cloudfunctions/createCustomerInvite`
  - `cloudfunctions/reviewDriverQuote`
  - `cloudfunctions/createCustomerQuoteDraft`
  - `cloudfunctions/publishCustomerQuotesBatch`
  - `cloudfunctions/getCustomerTransportQuotes`
  - `cloudfunctions/selectCustomerQuote`
  - `cloudfunctions/getRequestDetail/index.js`
  - `cloudfunctions/selectDriverQuote/index.js`
  - `miniprogram/pages/operator/request-detail/*`
  - `miniprogram/pages/customer/transfer-detail/*`
  - `miniprogram/pages/customer/home/*`
  - `miniprogram/pages/index/*`
- Notes:
  - Operators can send both driver and customer Mini Program cards.
  - Customer invite links bind by `cloud.getWXContext().OPENID`.
  - Operator can publish curated customer quote cards from reviewed driver quotes.
  - Customer can choose a driver option, which remains pending until operator confirmation.
  - Operator confirmation assigns the driver and exposes customer-safe driver details.
  - Operator driver-unavailable rejection cancels the selected customer quote and shows a customer reselect notice.
  - Customer pages still do not read `driver_quotes` directly.
- Next recommended task:
  - Deploy all changed cloud functions, then run the full operator-driver-customer QA checklist in WeChat DevTools.

### 2026-05-21

- Feature completed: added invite-only customer access for transfer quote detail.
- Files changed:
  - `cloudfunctions/createCustomerInvite`
  - `cloudfunctions/getCustomerTransportQuotes`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.js`
  - `miniprogram/pages/customer/transfer-detail/transfer-detail.wxml`
  - `TODO.md`
- Notes:
  - Operators can create one-time customer invite links for a `ride_requests` record.
  - First customer access claims the invite with `cloud.getWXContext().OPENID` and creates or updates a `customer` user record.
  - Already-claimed invites can be reopened only by the same customer OpenID.
  - Transfer Detail now calls `getCustomerTransportQuotes` instead of mock `getCustomerHome` data.
  - Customer pages still read only `customer_transport_quotes`; they never read `driver_quotes`.
- Next recommended task:
  - Add minimal operator UI to create/copy customer invite links after quotes are published.

### 2026-05-21

- Feature completed: added P1.2A backend foundation cloud functions for reviewed customer quote publishing.
- Files changed:
  - `cloudfunctions/reviewDriverQuote`
  - `cloudfunctions/createCustomerQuoteDraft`
  - `cloudfunctions/publishCustomerQuotesBatch`
  - `cloudfunctions/getCustomerTransportQuotes`
  - `TODO.md`
- Notes:
  - No mini-program UI, app routing, tabBar, quick-quote, or legacy `selectDriverQuote` logic was changed.
  - New functions keep operator review state separate from legacy `driver_quotes.quote_status`.
  - Customer-facing quote reads use `customer_transport_quotes` and never read `driver_quotes`.
  - Helpers are local to each cloud function directory so deployment does not depend on root-level shared code.
- Next recommended task:
  - Implement the minimal operator request-detail controls for review, draft creation, and batch publish.

### 2026-05-21

- Feature completed: documented P1.1 data boundary between internal driver quotes and customer-facing curated quotes.
- Files changed:
  - `AGENTS.md`
  - `docs/product/context.md`
  - `docs/product/p1-1-data-boundary-customer-quotes.md`
  - `TODO.md`
- Notes:
  - `driver_quotes` is explicitly internal-only.
  - `customer_transport_quotes` is the only future customer-visible quote source.
  - Operator review is required before customer quote publishing.
  - Farland service fee 10% must be calculated in Cloud Functions only.
- Next recommended task:
  - Keep the current ICT demo stable; do not implement P1.1 backend until explicitly requested.

### 2026-05-21

- Feature completed: stabilized customer entry and simplified My Trip home for demo review.
- Files changed:
  - `miniprogram/app.json`
  - `miniprogram/pages/index/index.js`
  - `miniprogram/pages/hotel/request/request.js`
  - `miniprogram/pages/hotel/request/request.wxml`
  - `miniprogram/pages/hotel/request/request.wxss`
  - `miniprogram/pages/customer/home/home.wxml`
  - `docs/TEST_CASES.md`
- Notes:
  - Hotel page is now the first registered page.
  - Index remains only as token fallback and immediately routes normal entry to the hotel tab.
  - Hotel requests no longer silently submit placeholder contact data.
  - My Trip home now keeps quote/charter detail out of the homepage and uses `我的用车` as the summary entry.
- Next recommended task:
  - Run WeChat DevTools preview and manually verify tabBar, hotel request contact prompt, and transfer detail navigation.

### 2026-05-21

- Feature completed: added project AI instructions and transport product context docs.
- Files changed:
  - `AGENTS.md`
  - `docs/product/context.md`
  - `docs/product/farland-student-transport-itinerary-spec.md`
  - `TODO.md`
- Notes:
  - Farland pricing rule is now explicit: driver/fleet quote + Farland service fee 10%.
  - Client-facing transport is defined as curated service options, not raw driver bidding.
- Next recommended task:
  - Run manual QA using `docs/TEST_CASES.md`, then fix UI issues discovered in WeChat DevTools.
